import { GameResolverInterceptor } from './game-resolver.interceptor';

describe('GameResolverInterceptor', () => {
  it('should be defined', () => {
    expect(new GameResolverInterceptor()).toBeDefined();
  });
});
